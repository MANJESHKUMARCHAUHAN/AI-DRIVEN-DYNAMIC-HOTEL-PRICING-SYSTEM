# Runtime dependencies -- Python 3.11
#
# Versions are pinned exactly. An ML system whose numbers change because a
# transitive dependency moved is not reproducible, and reproducibility is the
# whole point of recording a dataset hash alongside each model.
#
# Development-only tools live in requirements-dev.txt so the container image
# does not ship a test runner and a linter.

# --------------------------------------------------------------------------- #
# API
# --------------------------------------------------------------------------- #
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# --------------------------------------------------------------------------- #
# Database
# --------------------------------------------------------------------------- #
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# --------------------------------------------------------------------------- #
# Data
# --------------------------------------------------------------------------- #
# numpy is held below 2.0: Prophet's compiled extensions and the scikit-learn
# build pinned here are built against the 1.x ABI.
numpy==1.26.4
pandas==2.2.3

# --------------------------------------------------------------------------- #
# Streaming
# --------------------------------------------------------------------------- #
# kafka-python 2.0.2 imports `kafka.vendor.six.moves`, which breaks on Python
# 3.12+. Python 3.11 is therefore a hard floor *and* ceiling for this pin --
# one of the concrete reasons the project targets 3.11 exactly.
kafka-python==2.0.2

# --------------------------------------------------------------------------- #
# Machine learning
# --------------------------------------------------------------------------- #
scikit-learn==1.5.2
prophet==1.1.6
cmdstanpy==1.2.4
holidays==0.63
joblib==1.4.2

# --------------------------------------------------------------------------- #
# Ingestion
# --------------------------------------------------------------------------- #
beautifulsoup4==4.12.3
lxml==5.3.0
requests==2.32.3
httpx==0.28.1

# --------------------------------------------------------------------------- #
# Dashboard
# --------------------------------------------------------------------------- #
streamlit==1.41.1
plotly==5.24.1

# --------------------------------------------------------------------------- #
# Utilities
# --------------------------------------------------------------------------- #
python-dotenv==1.0.1
tenacity==9.0.0
