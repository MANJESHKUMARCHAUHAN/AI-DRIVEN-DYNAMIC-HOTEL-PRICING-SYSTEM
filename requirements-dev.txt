# Development and CI dependencies.
#
# Kept out of requirements.txt so the runtime image does not ship a test runner,
# a linter and a type checker. Install locally with:
#
#     pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.0

ruff==0.8.6
mypy==1.14.1
types-requests==2.32.0.20241016
